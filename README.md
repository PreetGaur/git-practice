# github learning

- git init:
- git status:
- git add
- git commit
- git branch
- git merge
- git checkout -b
- git log
- git stash

#git commit messages
- either use vim
- or use git commit -m "<messages>"

#merging

merging means to merge
